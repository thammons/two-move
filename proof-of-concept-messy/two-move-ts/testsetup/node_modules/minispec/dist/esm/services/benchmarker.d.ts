export default class Benchmarker {
    private startedAt;
    start(timeInMilliseconds?: number): void;
    getRawDuration(timeInMilliseconds?: number): number;
    getDuration(): number;
    protected static getActualTime(timeInMilliseconds?: number): number;
}
